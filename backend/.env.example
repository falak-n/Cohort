PORT=5000
# Example MongoDB URI for Atlas. Replace placeholders and DO NOT commit secrets.
# Use your encoded password if it has special characters (see encodeURIComponent).
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.qufnkvj.mongodb.net/<your_db>?retryWrites=true&w=majority
GEMINI_API_KEY=<your_api_key>
PORT=5000
# MongoDB URI format for Atlas
# Replace <username>, <password>, and <dbname> before using
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.qufnkvj.mongodb.net/<dbname>?retryWrites=true&w=majority
# Example local MongoDB (uncomment to use locally)
# MONGODB_URI=mongodb://localhost:27017/cohort_db

# Third-party API keys (redact in real .env)
GEMINI_API_KEY=<your_api_key_here>
